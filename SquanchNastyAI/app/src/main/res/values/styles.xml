<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:windowBackground">@color/background</item>
        <item name="android:textColor">@color/textColor</item>
    </style>

    <!-- Chat mode theme. -->
    <style name="ChatModeTheme" parent="AppTheme">
        <item name="android:windowBackground">@color/chatModeBackground</item>
        <item name="android:textColor">@color/chatModeTextColor</item>
    </style>

    <!-- Developer mode theme. -->
    <style name="DeveloperModeTheme" parent="AppTheme">
        <item name="android:windowBackground">@color/developerModeBackground</item>
        <item name="android:textColor">@color/developerModeTextColor</item>
    </style>

</resources>